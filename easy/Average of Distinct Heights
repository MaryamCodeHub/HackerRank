# HackerRank Task: Average of Distinct Heights
# Difficulty: Easy
# Task: Remove duplicate heights and calculate the average.
# Output: Average rounded to 3 decimal places
# Concept: set(), sum(), len(), round()

def average(array):
    distinct = set(array)
    avg = sum(distinct) / len(distinct)
    return round(avg, 3)

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    print(average(arr))
