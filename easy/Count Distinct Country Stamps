# HackerRank Task: Set .add() Operation
# Difficulty: Easy
# Task: Count total number of distinct country stamps.
# Output: Number of unique countries
# Concept: set(), add(), len()

n = int(input())
countries = set()

for _ in range(n):
    countries.add(input().strip())

print(len(countries))
